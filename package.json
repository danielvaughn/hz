{
	"name": "hz-monorepo",
	"private": true,
	"version": "0.0.1",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "npm run dev --workspace=hz",
		"dev:web": "npm run dev --workspace=hz",
		"dev:electron": "npm run dev --workspace=@hz/electron",
		"build": "npm run build --workspaces --if-present",
		"build:electron": "npm run build --workspace=@hz/electron",
		"preview": "npm run preview --workspace=hz",
		"check": "npm run check --workspaces --if-present",
		"check:watch": "npm run check:watch --workspace=hz"
	}
}
